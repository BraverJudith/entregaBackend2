 <h1>Detalle del Producto</h1>
    <div>
        <h2>{{product.title}}</h2>
        <p>ID: {{product._id}}</p>
        <p>{{product.description}}</p>
        <p>Precio: ${{product.price}}</p>
        <p>Categoría: {{product.category}}</p>
        <a href="/api/product?page=1" class="button-link">Volver a la lista de productos</a>
    </div>